<h2>Add a review</h2>

<%= simple_form_for([@restaurant, @review]) do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit %>
<% end %>



<p><%= link_to "Back to the list", restaurants_path(@restaurant) %></p>
